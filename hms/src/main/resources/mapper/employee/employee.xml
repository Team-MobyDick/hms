<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mobydick.hms.employee.dao.EmployeeDAO">

    <select id="selectAllEmployees" resultType="com.mobydick.hms.employee.vo.EmployeeVO">
        <![CDATA[
        SELECT
            EMPL_ID,
            EMPL_NAME,
            EMPL_PHONE,
            EMPL_DEPT,
            EMPL_GRADE,
            PHOTO_NAME,
            PHOTO_PATH,
            EMPL_NOTES,
            CREATED_DATE,
            CREATED_ID,
            UPDATED_DATE,
            UPDATED_ID
        FROM TB_EMPL
        ]]>
    </select>

    <insert id="insertEmployee" parameterType="com.mobydick.hms.employee.vo.EmployeeVO">
        <![CDATA[
        INSERT INTO TB_EMPL (
            EMPL_ID,
            EMPL_NAME,
            EMPL_PHONE,
            EMPL_DEPT,
            EMPL_GRADE,
            PHOTO_NAME,
            PHOTO_PATH,
            EMPL_NOTES,
            CREATED_DATE,
            CREATED_ID,
            UPDATED_DATE,
            UPDATED_ID
        ) VALUES (
            #{emplId},
            #{emplName},
            #{emplPhone},
            #{emplDept},
            #{emplGrade},
            #{photoName, jdbcType=VARCHAR},  #{photoPath, jdbcType=VARCHAR},  #{emplNotes},
            SYSDATE,
            #{createdId},
            SYSDATE,
            #{createdId}
        )
        ]]>
    </insert>

    <select id="countEmployeeById" parameterType="string" resultType="int">
        <![CDATA[
        SELECT COUNT(*)
        FROM TB_EMPL
        WHERE EMPL_ID = #{emplId}
        ]]>
    </select>

</mapper>